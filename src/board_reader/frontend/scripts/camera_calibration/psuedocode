To calibrate the camera

#Take in input from the user interface/config:
    #Calibration Images Path
    #Calibration Save Path
    #Camera Type (Pinhole/Fisheye/Normal)
    #Charuco Board Parameters
        #Vertical Squares
        #Horizontal Squares
        #Square Length (meters)
        #Marker Length (meters)

#Load the virtual environment and run the setup scripts:
    #Utilizing the input run the calibration using the user input
    #Save the calibration and then

